ref: usage-examples-template
content: |

  .. default-domain:: mongodb

  .. contents:: On this page
     :local:
     :backlinks: none
     :depth: 2
     :class: singlecol

  You can {{youCanDoWhat}} using the ``{{methodName}}()`` method{{optionalAsyncMethod}} on
  a collection object.  

  Example
  -------

  This example uses the following ``Restaurant`` class as a model:

  .. literalinclude:: ../includes/code-examples/{{codeLabel}}/Restaurant.cs
     :start-after: start-model
     :end-before: end-model
     :language: csharp
     :copyable:
     :dedent:

  The following code {{codeDoesWhat}}. 

  Select the :guilabel:`Asynchronous` or :guilabel:`Synchronous` tab to see the 
  corresponding code.

  .. tabs::

     .. tab:: Asynchronous
        :tabid: {{codeLabel}}-async

        .. literalinclude:: ../includes/code-examples/{{codeLabel}}/{{methodNameAsync}}.cs
           :start-after: start-{{codeLabel}}-async
           :end-before: end-{{codeLabel}}-async
           :language:  csharp
           :copyable:
           :dedent:

        For a fully runnable example of the ``{{methodNameAsync}}()`` operation, see the
        `{{methodNameAsync}} code sample <{+example+}/{{codeLabel}}/{{methodNameAsync}}.cs>`__.

     .. tab:: Synchronous
        :tabid: {{codeLabel}}-sync

        .. literalinclude:: ../includes/code-examples/{{codeLabel}}/{{methodName}}.cs
           :start-after: start-{{codeLabel}}
           :end-before: end-{{codeLabel}}
           :language:  csharp
           :copyable:
           :dedent:
      
        For a fully runnable example of the ``{{methodName}}()`` operation, see the
        `{{methodName}} code sample <{+example+}/{{codeLabel}}/{{methodName}}.cs>`__.

  Expected Result
  ~~~~~~~~~~~~~~~

  Running either of the preceding full examples prints the following results:

  .. code-block:: none

     {{codeResultLineOne}}
     {{optionalCodeResultLineTwo}}
     {{optionalCodeResultLineThree}}
     {{optionalCodeResultLineFour}}

  More Information
  ----------------

  .. TODO: Add links to Fundamentals once pages are done

  {{buildersLink}}

  {{linqLink}}

  API Documentation
  -----------------

  {{firstApi}}
  {{optionalSecondApi}}
  {{optionalThirdApi}}
  {{optionalFourthApi}}

# replacement:
#   codeDoesWhat: "test value"
#   methodNameAsync: "UpdateManyAsync"
#   optionalAsyncMethod: ""
#   optionalSecondApi: ""
#   optionalThirdApi: ""
#   optionalFourthApi: ""
#   buildersLink: ""
#   linqLink: ""

